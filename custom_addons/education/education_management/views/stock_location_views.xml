<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- ACTION WINDOW UNTUK CLASSROOM PROPERTY  -->
    <record id="education_classroom_property_action" model="ir.actions.act_window">
        <field name="name">Inventory Classroom Property</field>
        <field name="res_model">stock.location</field>
        <field name="view_mode">list,form</field>
        <field name="domain">[('storage_category_id', '=', 'Classroom Property')]</field>
        <field name="context">{'storage_category_id': 'Classroom Property'}</field>
    </record>

</odoo>
