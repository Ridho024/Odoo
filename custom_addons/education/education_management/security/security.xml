<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <data noupdate="1">
    
        <!-- Groups -->
        <record id="group_education_manager" model="res.groups">
            <field name="name">Education Manager</field>
            <field name="category_id" ref="base.module_category_human_resources"/>
        </record>

        <record id="group_education_teacher" model="res.groups">
            <field name="name">Education Teacher</field>
            <field name="category_id" ref="base.module_category_human_resources"/>
        </record>

        <!-- Record Rule: Teacher can only see their classroom -->
        <record id="rule_teacher_classroom_own" model="ir.rule">
            <field name="name">Teacher: See Own Classroom</field>
            <field name="model_id" ref="education_management.model_education_student_attendance" />
            <field name="domain_force">[('teacher_id.user_id', '=', user.id)]</field>
            <field name="groups" eval="[(4, ref('education_management.group_education_teacher'))]"/>
        </record>

        <!-- Record Rule: Restrict access to create for this group -->
        <!-- <record id="rule_res_partner_teacher_read_only" model="ir.rule">
            <field name="name">Teachers cannot create new partners (Education Context)</field>
            <field name="model_id" ref="base.model_res_partner"/>
            <field name="groups" eval="[(4, ref('education_management.group_education_teacher'))]"/>
            <field name="domain_force">[('is_teacher', '=', True)]</field>
            <field name="perm_read" eval="1"/>
            <field name="perm_write" eval="0"/>
            <field name="perm_create" eval="0"/>
            <field name="perm_unlink" eval="0"/>
        </record> -->
    </data>
</odoo>
